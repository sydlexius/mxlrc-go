---
phase: 04-build-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - .goreleaser.yml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03

must_haves:
  truths:
    - "`make build` produces a binary named `mxlrcsvc-go` from `cmd/mxlrcsvc-go/`"
    - "GoReleaser config builds from `cmd/mxlrcsvc-go/` and names the binary `mxlrcsvc-go`"
    - "CI matrix build step uses `./cmd/mxlrcsvc-go` as the build path and outputs `mxlrcsvc-go`"
  artifacts:
    - path: "Makefile"
      provides: "Updated BINARY and build target"
      contains: "BINARY=mxlrcsvc-go"
    - path: ".goreleaser.yml"
      provides: "Updated GoReleaser build config"
      contains: "cmd/mxlrcsvc-go"
    - path: ".github/workflows/ci.yml"
      provides: "Updated CI matrix build step"
      contains: "mxlrcsvc-go"
  key_links:
    - from: "Makefile"
      to: "cmd/mxlrcsvc-go/"
      via: "go build -o $(BINARY) ./cmd/mxlrcsvc-go"
      pattern: "go build.*cmd/mxlrcsvc-go"
    - from: ".goreleaser.yml"
      to: "cmd/mxlrcsvc-go/"
      via: "main: ./cmd/mxlrcsvc-go"
      pattern: "main:.*cmd/mxlrcsvc-go"
---

<objective>
Update all build tooling to reference the new binary name (`mxlrcsvc-go`) and build path (`./cmd/mxlrcsvc-go/`).

Purpose: The restructuring moved the entry point from the root package to `cmd/mxlrcsvc-go/`. Build tools still point to the old location and binary name — this plan corrects all three: Makefile, GoReleaser, and CI.
Output: Three updated config files that correctly build `mxlrcsvc-go` from `./cmd/mxlrcsvc-go/`.
</objective>

<execution_context>
@/Users/jesse/Developer/mxlrc-go/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jesse/Developer/mxlrc-go/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Current state that needs updating -->

Makefile (current — WRONG):
```makefile
BINARY=mxlrc-go
build:
	go build -o $(BINARY) .
```

.goreleaser.yml (current — WRONG):
```yaml
builds:
  - id: mxlrc-go
    main: .
    binary: mxlrc-go
```

.github/workflows/ci.yml build-matrix step (current — WRONG):
```bash
run: go build -ldflags="-s -w" -o mxlrc-go .
```

Target state:
- Binary name everywhere: `mxlrcsvc-go`
- Build path everywhere: `./cmd/mxlrcsvc-go`
- GoReleaser release.github.name: `mxlrcsvc-go` (GitHub repo renamed to match)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Makefile for new binary name and build path</name>
  <files>Makefile</files>
  <action>
Edit Makefile with the following changes:

1. `BINARY=mxlrc-go` → `BINARY=mxlrcsvc-go`
2. `go build -o $(BINARY) .` → `go build -o $(BINARY) ./cmd/mxlrcsvc-go`

No other changes needed. The `clean` target already uses `$(BINARY)` so it will clean the right file. The `run` target already uses `./$(BINARY)` so it stays correct.

Per BUILD-01.
  </action>
  <verify>
    <automated>grep -E "BINARY=mxlrcsvc-go" Makefile && grep -E "cmd/mxlrcsvc-go" Makefile && make build && test -f mxlrcsvc-go && echo "PASS"</automated>
  </verify>
  <done>`make build` succeeds and produces `./mxlrcsvc-go` binary from `cmd/mxlrcsvc-go/`</done>
</task>

<task type="auto">
  <name>Task 2: Update GoReleaser config for new binary name and build path</name>
  <files>.goreleaser.yml</files>
  <action>
Edit `.goreleaser.yml` with the following changes:

1. `id: mxlrc-go` → `id: mxlrcsvc-go`
2. `main: .` → `main: ./cmd/mxlrcsvc-go`
3. `binary: mxlrc-go` → `binary: mxlrcsvc-go`
4. `release.github.name: mxlrc-go` → `release.github.name: mxlrcsvc-go`

Per BUILD-02. The `ldflags: -s -w` and all platform matrix settings remain unchanged.
  </action>
  <verify>
    <automated>grep "id: mxlrcsvc-go" .goreleaser.yml && grep "main: ./cmd/mxlrcsvc-go" .goreleaser.yml && grep "binary: mxlrcsvc-go" .goreleaser.yml && echo "PASS"</automated>
  </verify>
  <done>GoReleaser config references `mxlrcsvc-go` as binary name and `./cmd/mxlrcsvc-go` as main path</done>
</task>

<task type="auto">
  <name>Task 3: Update CI workflow build step for new path and binary name</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Edit `.github/workflows/ci.yml`, find the `build-matrix` job's `Build binary` step:

Current:
```
run: go build -ldflags="-s -w" -o mxlrc-go .
```

Replace with:
```
run: go build -ldflags="-s -w" -o mxlrcsvc-go ./cmd/mxlrcsvc-go
```

Only this one line changes. All other CI workflow steps remain unchanged.

Per BUILD-03.
  </action>
  <verify>
    <automated>grep "mxlrcsvc-go ./cmd/mxlrcsvc-go" .github/workflows/ci.yml && echo "PASS"</automated>
  </verify>
  <done>CI build-matrix step uses correct binary name and build path</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config files → build tools | Build tool reads config; malformed config could produce incorrect binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Makefile / .goreleaser.yml | accept | Config files in version control; no secrets involved; worst outcome is failed build, not security issue |
| T-04-01-02 | Information Disclosure | .github/workflows/ci.yml | accept | CI builds public repo; no secrets in these config changes |
</threat_model>

<verification>
After all three tasks:

```bash
# Verify Makefile
make build && test -f mxlrcsvc-go && echo "Binary exists"
make clean && test ! -f mxlrcsvc-go && echo "Clean works"

# Verify GoReleaser config is valid YAML with correct fields
grep -c "mxlrcsvc-go" .goreleaser.yml  # Should be >= 3 (id, binary, release name)

# Verify CI config
grep "mxlrcsvc-go ./cmd/mxlrcsvc-go" .github/workflows/ci.yml

# Full build check
go build ./... && echo "Module builds clean"
```
</verification>

<success_criteria>
- `make build` produces `./mxlrcsvc-go` (not `mxlrc-go`)
- `make clean` removes `./mxlrcsvc-go`
- `.goreleaser.yml` has `id: mxlrcsvc-go`, `main: ./cmd/mxlrcsvc-go`, `binary: mxlrcsvc-go`
- CI `build-matrix` step builds from `./cmd/mxlrcsvc-go` with output `mxlrcsvc-go`
- `go build ./...` still passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-verification/04-01-SUMMARY.md`
</output>
